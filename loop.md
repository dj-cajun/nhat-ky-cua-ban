# loop.md — 작업 감독관 (Loop Supervisor)

> **역할**: AI가 어떤 태스크를 수행하든, "완료"를 선언하기 전에 이 문서의 검증 루프를 반드시 통과해야 합니다.
> **프로젝트**: Nhật ký của bạn

---

## 0. 참조 설계 문서

작업 전·후 반드시 대조할 문서:

| 문서 | 경로 |
|------|------|
| PRD | `docs/PRD.md` |
| TRD | `docs/TRD.md` |
| User Flow | `docs/user-flow.md` |
| DB Design | `docs/db-design.md` |
| Screens | `docs/screens.md` |
| Tasks | `docs/tasks.md` |
| Coding Conventions | `docs/coding-conventions.md` |

**충돌 발견 시**: 작업을 완료로 표시하지 말고, 충돌 내용과 수정안을 보고한 뒤 인간 승인을 요청.

---

## 1. Loop 프로세스 (매 태스크마다 실행)

```
┌─────────────┐
│ 1. 태스크   │  tasks.md에서 현재 태스크 확인
│    확인     │
└──────┬──────┘
       ▼
┌─────────────┐
│ 2. 설계     │  PRD/TRD/UserFlow/DB/Screens와 정합성 검토
│    대조     │
└──────┬──────┘
       ▼
┌─────────────┐
│ 3. 구현     │  coding-conventions.md 준수
└──────┬──────┘
       ▼
┌─────────────┐
│ 4. Pass/Fail│  §2.1 — 하나라도 실패 → 수정 후 재검증
│    검증     │
└──────┬──────┘
       ▼
┌─────────────┐
│ 5. 측정     │  §2.2 — 수치 기준 확인
│    검증     │
└──────┬──────┘
       ▼
┌─────────────┐
│ 6. 자가평가 │  §2.3 — 점수 + 감점 근거 + 수정 액션
└──────┬──────┘
       ▼
┌─────────────┐
│ 7. 완료     │  tasks.md 상태 업데이트 → 다음 태스크
│    보고     │
└─────────────┘
```

---

## 2. 검증 기준

### 2.1 필수 통과 기준 (Pass/Fail) — 하나라도 FAIL이면 미완료

| # | 검사 항목 | 명령어 / 방법 | Pass 조건 |
|---|-----------|---------------|-----------|
| P-01 | 빌드 | `npm run build` | exit 0 |
| P-02 | 타입 체크 | `npm run typecheck` 또는 `tsc --noEmit` | 오류 0 |
| P-03 | 린트 | `npm run lint` | 오류 0 |
| P-04 | 테스트 | `npm run test` | 전체 통과 |
| P-05 | 시크릿 노출 | `.env` git 추적 여부, 하드코딩 키 grep | 노출 없음 |
| P-06 | 설계 정합성 | PRD·UserFlow·Screens·DB 대조 | 충돌 없음 |

### 2.2 측정 기준 (수치화)

| # | 지표 | 목표 | 측정 방법 |
|---|------|------|-----------|
| M-01 | 테스트 커버리지 | ≥ 60% (유틸/validation) | `npm run test:coverage` |
| M-02 | Lighthouse Performance | ≥ 80 (주요 페이지) | Chrome DevTools |
| M-03 | Lighthouse Accessibility | ≥ 90 | Chrome DevTools |
| M-04 | API/페이지 응답 | < 2초 (로컬) | 수동 또는 Playwright |
| M-05 | 번들 크기 증가 | < 50KB (gzip, 태스크당) | `npm run build` 분석 |

미달 시: 수치, 원인, 개선 액션을 완료 보고에 포함.

### 2.3 평가 기준 (AI 자가 평가 + 근거)

완료 보고 시 아래 항목을 **1~5점**으로 평가하고, **4점 미만 항목은 감점 근거 + 수정 액션**을 필수 작성.

| # | 평가 항목 | 질문 |
|---|-----------|------|
| E-01 | 아키텍처 적절성 | TRD 구조·패턴을 따랐는가? |
| E-02 | 유저 플로우 자연스러움 | user-flow.md 흐름과 일치하는가? |
| E-03 | UX 완성도 | screens.md 정의와 일치하는가? |
| E-04 | 에러 처리 | 엣지 케이스가 처리되었는가? |
| E-05 | 코드 품질 | conventions 준수, 읽기 쉬운가? |
| E-06 | 보안 | RLS, 입력 검증, 인증 경계가 지켜졌는가? |

**평가 보고 형식 (예시)**:

```
E-03 UX 완성도: 3/5
- 감점 근거: 삭제 확인 모달이 screens.md의 "모달" 정의와 달리 inline alert 사용
- 수정 액션: ConfirmDialog 컴포넌트로 교체
```

---

## 3. AI 자동 처리 vs 인간 개입 경계

### 3.1 AI가 자율 처리 가능

- 린트/타입 오류 수정
- 누락된 단위 테스트 추가
- 문서 오타·동기화 (tasks.md 상태 등)
- 단순 네이밍·import 정리
- CI 단순 실패 (의존성, 포맷) 수정 후 재푸시

### 3.2 인간 호출 필수 (승인 전 진행 금지)

| 상황 | 이유 |
|------|------|
| DB 스키마 변경 (컬럼 추가/삭제) | db-design.md 변경 필요 |
| PRD 범위 변경 (기능 추가/삭제) | 제품 방향 결정 |
| 결제·인증 방식 변경 | 보안 영향 |
| TRD 기술 스택 변경 | 아키텍처 결정 |
| RLS 정책 완화 | 보안 위험 |
| `.env` / 시크릿 값 설정 | 민감 정보 |

**호출 형식**: `🚨 인간 개입 필요: [상황] / [제안] / [영향]`

---

## 4. 운영 감시 루프 (PR/CI)

PR 생성 후 AI가 주기적으로 수행:

```
1. PR 체크 상태 확인 (gh pr checks)
2. 실패 시:
   a. 로그 분석
   b. §3.1 자동 처리 가능 → 수정 → 커밋 → 푸시
   c. §3.2 해당 → 인간에게 보고 후 대기
3. 모든 체크 통과 시 → 머지 준비 보고서 제출
```

### 머지 준비 보고서 템플릿

```markdown
## 머지 준비 보고서

- PR: #번호
- 태스크: T-XX
- Pass/Fail: P-01~P-06 전체 PASS
- 측정: M-01~M-05 결과 요약
- 자가평가: E-01~E-06 점수표
- 미해결 이슈: (없음 / 목록)
- 권장: 머지 가능 / 추가 검토 필요
```

---

## 5. 완료 보고서 템플릿 (태스크 단위)

태스크 완료 시 아래 형식으로 보고:

```markdown
## 태스크 완료 보고: T-XX [제목]

### 변경 파일
- path/to/file.tsx

### 설계 정합성
- [x] PRD F-XX 충족
- [x] User Flow §2 일치
- [x] Screen S-XX 구현

### Pass/Fail
| 항목 | 결과 |
|------|------|
| P-01 빌드 | PASS |
| ... | ... |

### 측정
| 항목 | 결과 | 목표 |
|------|------|------|
| M-01 커버리지 | 65% | 60% |

### 자가평가
| 항목 | 점수 | 근거/액션 |
|------|------|-----------|
| E-01 | 5/5 | — |

### 다음 태스크
T-YY
```

---

## 6. 현재 상태

| 항목 | 값 |
|------|-----|
| 현재 Phase | Phase 0 + Phase 2 셸 |
| 현재 태스크 | T-00~T-01, T-20~T-26, T-42 진행 중 |
| 마지막 Loop 통과 | — |
| 블로커 | Supabase 프로젝트 미연결 (T-02) |

---

*이 파일은 프로젝트의 최상위 감독관입니다. AI는 "완료"를 선언하기 전에 반드시 이 루프를 실행하세요.*
