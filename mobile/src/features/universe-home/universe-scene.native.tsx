/**
 * Native uses the same 2D pastel diagram as web.
 * Dropping R3F/three/expo-gl here keeps install size down; visuals stay matched.
 */
export { UniverseScene3D, type UniverseSceneProps } from './universe-scene';
